#include <stdio.h>

int main() {
    int i,n, sum = 0;
    printf("Enter the value of n: ");
    scanf("%d", &n);
    for (i = 1; i <= n; i++) 
	{
        sum += i * 11;
    }
    printf("Sum of the sequence 11+22+33+44+...+%d is %d", n, sum);
    return 0;
}
